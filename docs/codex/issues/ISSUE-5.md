# Codex Directive — Issue #5

Read `AGENTS.md`, `docs/CODEX-START-HERE.md`, `docs/CODEX-EXECUTION-PLAN.md`, Issue #5, the canonical registry, legacy calculator/audit modules, fixtures, research memoranda, and source records. Migrate all ten tools into versioned, source-linked, reproducible specifications and pure deterministic engines. For each tool implement typed/provenanced inputs, effective dates, source/rule dependencies, explicit predicates, branch trace, intermediate math, outputs, assumptions, limitations, ambiguity/unsupported states, worked examples, boundary and regression fixtures, technical/legal review states, serialization, save-to-property/matter contracts, and revalidation behavior. Differentially test against legacy and classify every mismatch rather than assuming either side is correct. Open a PR titled `Extract ten governed wage-hour calculation engines`, repair remote CI, and update Issue #5 with evidence. Do not close until all ten tools satisfy every acceptance criterion.